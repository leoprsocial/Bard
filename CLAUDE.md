# CLAUDE.md

## Repository Overview

**Bard** is a project hosted at `github.com/leoprsocial/Bard`. It is currently in its initial stage with no application code yet.

## Repository Structure

```
Bard/
├── BARDLEO          # Repository setup notes (git init instructions)
└── CLAUDE.md        # This file — guidance for AI assistants
```

## Git Workflow

- **Default branch:** `master`
- Develop on feature branches and merge via pull requests.
- Write clear, descriptive commit messages.

## Development Guidelines

- Keep the repository clean — avoid committing generated files, build artifacts, or secrets.
- Prefer editing existing files over creating new ones to prevent file bloat.
- Do not commit `.env` files, credentials, or API keys.

## Notes for AI Assistants

- This repository is in its earliest stage. When adding code, confirm the intended language, framework, and project structure with the user first.
- Read existing files before proposing modifications.
- Follow any conventions established by future commits.
